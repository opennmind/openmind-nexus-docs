
import { BasicDocsPage } from "../templates/BasicDocsPage";

const Installation = () => {
  return (
    <BasicDocsPage
      title="Installation"
      description="Get started with OpenMind by installing the package and its dependencies."
    />
  );
};

export default Installation;
