
import { BasicDocsPage } from "../templates/BasicDocsPage";

const ResponsiveDesign = () => {
  return (
    <BasicDocsPage
      title="Responsive Design"
      description="Build responsive interfaces that look great on any device with OpenMind."
    />
  );
};

export default ResponsiveDesign;
